<UserControl x:Class="TimeLeaf.Views.Workspace.TaskFlowNodeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TimeLeaf.ViewModels"
             xmlns:wv="clr-namespace:TimeLeaf.Views.Workspace"
             d:DataContext="{d:DesignInstance vm:ProjectTaskViewModel}"
             mc:Ignorable="d">
    <Grid>
        <Border x:Name="NodeBorder"
                MinWidth="140"
                Padding="12,8"
                Background="White"
                BorderBrush="{DynamicResource OutlineBaseBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <Border.InputBindings>
                <MouseBinding Command="{Binding RequestDetailCommand}"
                              MouseAction="LeftDoubleClick" />
            </Border.InputBindings>
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}"
                                     Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource BrandPrimaryBrush}" />
                            <Setter Property="BorderThickness" Value="2" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding HasRisk}"
                                     Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}" />
                            <Setter Property="BorderThickness" Value="2" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Border.Effect>
                <DropShadowEffect BlurRadius="10"
                                  Direction="-90"
                                  Opacity="{StaticResource OpacityMuted}"
                                  ShadowDepth="2" />
            </Border.Effect>
            <StackPanel>
                <TextBlock FontWeight="Bold"
                           Style="{StaticResource TextBlockLabelBaseStyle}"
                           Text="{Binding Name}"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock Margin="0,4,0,0"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           Style="{StaticResource TextBlockMetaBaseStyle}"
                           Text="{Binding Status}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
