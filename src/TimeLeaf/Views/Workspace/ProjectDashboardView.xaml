<UserControl x:Class="TimeLeaf.Views.Workspace.ProjectDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="http://schemas.leafkit.com/wpf/ui/controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TimeLeaf.ViewModels.Workspace"
             d:DataContext="{d:DesignInstance Type=vm:ProjectDashboardViewModel}"
             d:DesignHeight="600"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Style="{StaticResource ViewMainContainerStyle}">
            <!-- プロジェクト概要 -->
            <StackPanel Margin="{StaticResource ContentBottomMargin}">
                <TextBlock Style="{StaticResource DashboardDescriptionTextStyle}" Text="{Binding Description}" />
            </StackPanel>

            <!-- 統計情報 (Stats) -->
            <UniformGrid Margin="{StaticResource ContentBottomMargin}" Columns="4">
                <Border Margin="{StaticResource ContentRightMargin}" Style="{StaticResource DashboardCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Path Grid.Column="0"
                              Margin="{StaticResource ContentRightMargin}"
                              VerticalAlignment="Center"
                              Data="{StaticResource TaskIconGeometry}"
                              Style="{StaticResource BigIconPathStyle}" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource StatsLabelStyle}" Text="全タスク" />
                            <TextBlock Style="{StaticResource StatsValueStyle}" Text="{Binding TotalTaskCount}" />
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Margin="{StaticResource ContentRightMargin}" Style="{StaticResource DashboardCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Path Grid.Column="0"
                              Margin="{StaticResource ContentRightMargin}"
                              VerticalAlignment="Center"
                              Data="{StaticResource CompletedIconGeometry}"
                              Style="{StaticResource BigIconPathStyle}" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource StatsLabelStyle}" Text="完了済み" />
                            <TextBlock Style="{StaticResource StatsValueStyle}" Text="{Binding CompletedTaskCount}" />
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Margin="{StaticResource ContentRightMargin}" Style="{StaticResource DashboardCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <c:DonutChart Grid.Column="0"
                                      Width="20"
                                      Height="20"
                                      Margin="{StaticResource ContentRightMargin}"
                                      VerticalAlignment="Center"
                                      StrokeThickness="3"
                                      Value="{Binding CompletionPercentage}" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource StatsLabelStyle}" Text="進捗率" />
                            <TextBlock Style="{StaticResource StatsValueStyle}">
                                <TextBlock.Text>
                                    <MultiBinding Converter="{StaticResource StringFormatMultiConverter}" StringFormat="{}{0:F0}%">
                                        <Binding Mode="OneWay" Path="CompletionPercentage" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Style="{StaticResource DashboardCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Path Grid.Column="0"
                              Margin="{StaticResource ContentRightMargin}"
                              VerticalAlignment="Center"
                              Data="{StaticResource EffortIconGeometry}"
                              Style="{StaticResource BigIconPathStyle}" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Style="{StaticResource StatsLabelStyle}" Text="実績工数" />
                            <TextBlock Style="{StaticResource StatsValueStyle}">
                                <TextBlock.Text>
                                    <MultiBinding Converter="{StaticResource StringFormatMultiConverter}" StringFormat="{}{0:F1}h">
                                        <Binding Mode="OneWay" Path="TotalActualCost" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>
            </UniformGrid>

            <!-- マイルストーンと期限タスク -->
            <UniformGrid Columns="2">
                <StackPanel Margin="{StaticResource ContentRightMargin}">
                    <Grid Margin="{StaticResource ContentLabelMargin}">
                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                            <Path Margin="{StaticResource IconRightMargin}"
                                  VerticalAlignment="Center"
                                  Data="{StaticResource MilestoneIconGeometry}"
                                  Style="{DynamicResource IconPathStyle}" />
                            <TextBlock Margin="0"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource DashboardSectionHeaderStyle}"
                                       Text="マイルストーン" />
                        </StackPanel>
                        <Button HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Command="{Binding AddMilestoneCommand}"
                                Style="{StaticResource DashboardAddMilestoneButtonStyle}"
                                ToolTip="マイルストーン追加"
                                Visibility="{Binding IsAssignedToMe, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Path Data="{StaticResource AddIconGeometry}"
                                  Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                  Style="{DynamicResource SmallIconPathStyle}" />
                        </Button>
                    </Grid>
                    <Border Style="{StaticResource DashboardCardStyle}">
                        <ItemsControl ItemsSource="{Binding Milestones}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="16" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="20" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Rectangle Grid.RowSpan="3"
                                                   Grid.Column="0"
                                                   Style="{StaticResource HomeMilestoneTimelineLineStyle}" />
                                        <StackPanel Grid.Row="0"
                                                    Grid.Column="1"
                                                    Margin="8,0,0,2"
                                                    Orientation="Horizontal">
                                            <TextBlock Style="{StaticResource HomeMilestoneDateStyle}" Text="{Binding Date, StringFormat={}{0:MM/dd (ddd)}}" />
                                        </StackPanel>
                                        <Ellipse Grid.Row="1"
                                                 Grid.Column="0"
                                                 Style="{StaticResource HomeMilestoneTimelineDotStyle}" />
                                        <TextBlock Grid.Row="1"
                                                   Grid.Column="1"
                                                   Style="{StaticResource HomeMilestoneLabelStyle}"
                                                   Text="{Binding Label}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </StackPanel>

                <StackPanel>
                    <Grid Margin="{StaticResource ContentLabelMargin}">
                        <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                            <TextBlock Margin="{StaticResource IconRightMargin}"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource IconTextStyle}"
                                       Text="🚨" />
                            <TextBlock Margin="0"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource DashboardSectionHeaderStyle}"
                                       Text="期限が近いタスク" />
                        </StackPanel>
                    </Grid>
                    <Border Style="{StaticResource DashboardCardStyle}">
                        <ItemsControl ItemsSource="{Binding UpcomingTasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Style="{StaticResource HomeDeadlineItemStyle}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Text="{Binding Name}" />
                                        <TextBlock Grid.Column="1"
                                                   Foreground="{DynamicResource WarningBrush}"
                                                   Text="{Binding Deadline, StringFormat={}{0:MM/dd}}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </StackPanel>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
