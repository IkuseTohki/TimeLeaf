<UserControl x:Class="TimeLeaf.Views.Controls.TaskStatusPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:me="clr-namespace:TimeLeaf.Models.Enums;assembly=TimeLeaf"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             d:DesignHeight="50"
             d:DesignWidth="200"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="StatusValues"
                            MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="me:TaskStatus" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <ComboBox ItemsSource="{Binding Source={StaticResource StatusValues}}"
              SelectedItem="{Binding SelectedStatus, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
              Style="{DynamicResource ComboBoxFormCompactStyle}" />
</UserControl>
